package com.kevinputro.core.utils

import java.util.Locale

object Constant {
  val defaultLocale = Locale("in", "ID")

  const val FORMAT_UTC = "yyyy-MM-dd'T'HH:mm:ss'Z'"
  const val FORMAT_YEAR_ONLY = "yyyy"
}
